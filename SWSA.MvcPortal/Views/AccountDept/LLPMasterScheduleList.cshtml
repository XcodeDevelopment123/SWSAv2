@{
    ViewData["Title"] = "LLPMasterScheduleList";
}

<h1>LLP Master Scheduling List</h1>

<div class="row">
    <div class="col-md-12">
        <!-- Alert Container -->
        <div id="alertContainer"></div>

        <!-- /.card -->
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-tasks me-2"></i>LLP Master Schedule Records
                </h3>
                <div class="card-tools">
                    <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#taskModal" id="addNewBtn">
                        <i class="fas fa-plus me-2"></i>New Record
                    </button>
                </div>
            </div>
            <!-- /.card-header -->
            <div class="card-body" style="overflow:auto">
                <table id="taskDatatable" class="table table-bordered table-striped table-responsive">
                    <thead>
                        <!-- 第一行：主标题 -->
                        <tr>
                            <th colspan="3">Admin</th>
                            <th colspan="8">Co Information</th>
                            <th colspan="11">Scheduling</th>
                            <th rowspan="3">Actions</th>
                        </tr>

                        <!-- 第二行：细分标题 -->
                        <tr>
                            <th rowspan="2">Grouping</th>
                            <th rowspan="2">Referral</th>
                            <th rowspan="2">File No</th>

                            <th rowspan="2">Co Name</th>
                            <th rowspan="2">Year End</th>

                            <th colspan="2">Registration</th>
                            <th colspan="2">Tax Reference</th>
                            <th colspan="2">MISC Code</th>

                            <th colspan="4">Work Allocation Info</th>
                            <th colspan="1">Audit Dept</th>
                            <th colspan="1">Sec/Tax Dept</th>
                            <th colspan="5">Acc Dept</th>
                        </tr>

                        <!-- 第三行：每个字段的详细标题 -->
                        <tr>
                            <th>Incorp Date</th>
                            <th>Co #</th>

                            <th>E number</th>
                            <th>TIN number</th>

                            <th>Code</th>
                            <th>Description</th>

                            <th>Service Type</th>
                            <th>Co Status</th>
                            <th>Active Co:Activity/size</th>
                            <th>YE to do</th>

                            <th>Audit Dept Mth to do</th>

                            <th>Due Date</th>

                            <th>EST Month to do</th>
                            <th>Date Doc In</th>
                            <th>Staff</th>
                            <th>Allocate to Wk Sch</th>
                            <th>Completed/Backlog</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Data will be loaded dynamically -->
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>

<!-- 模态框部分 -->
<div class="modal fade" id="taskModal" tabindex="-1" aria-labelledby="taskModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="taskModalLabel">
                    <i class="fas fa-edit me-2"></i>
                    <span id="modalTitle">Add New LLP Record</span>
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="taskForm">
                    <input type="hidden" id="recordId" value="">

                    <div class="row">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="grouping">Grouping</label>
                                <input type="text" id="grouping" name="grouping" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="referral">Referral</label>
                                <input type="text" id="referral" name="referral" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="fileNo">File No</label>
                                <input type="text" id="fileNo" name="fileNo" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="companyName">Co Name</label>
                                <select id="companySelect" name="companySelect" class="form-control">
                                    <option value="">-- Select company --</option>
                                </select>
                                <small class="text-muted">
                                    Options are linked from the LLP Client List.
                                </small>

                                <!-- 真正要存进 B11 的公司名、ClientId 用隐藏字段 -->
                                <input type="hidden" id="company" name="company" />
                                <input type="hidden" id="companyClientId" name="companyClientId" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="yearEnd">Year End</label>
                                <input type="text" id="yearEnd" name="yearEnd" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="incorpDate">Incorp Date</label>
                                <input type="text" id="incorpDate" name="incorpDate" class="form-control datepicker" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="co">Co #</label>
                                <input type="text" id="co" name="co" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="enumber">E number</label>
                                <input type="text" id="enumber" name="enumber" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="tINnumber">TIN number</label>
                                <input type="text" id="tINnumber" name="tINnumber" class="form-control" placeholder="Auto fill from company" readonly />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="code">MISC Code</label>
                                <input type="text" id="code" name="code" class="form-control" placeholder="Enter MISC code" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="description">MISC Description</label>
                                <input type="text" id="description" name="description" class="form-control" placeholder="Enter description" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="servicesType">Service Type</label>
                                <input type="text" id="servicesType" name="servicesType" class="form-control" placeholder="Enter service type" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="coStatus">Co Status</label>
                                <input type="text" id="coStatus" name="coStatus" class="form-control" placeholder="Enter company status" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="activeCoActivitySize">Active Co:Activity/size</label>
                                <input type="text" id="activeCoActivitySize" name="activeCoActivitySize" class="form-control" placeholder="Enter activity/size" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="yEtodo">YE to do</label>
                                <input type="text" id="yEtodo" name="yEtodo" class="form-control" placeholder="Enter YE to do" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="auditDeptMth">Audit Dept Mth to do</label>
                                <input type="text" id="auditDeptMth" name="auditDeptMth" class="form-control" placeholder="Enter month" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="aDdueDate">Due Date</label>
                                <input type="text" id="aDdueDate" name="aDdueDate" class="form-control datepicker" placeholder="Select date" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="extensionDate">Extension Date</label>
                                <input type="text" id="extensionDate" name="extensionDate" class="form-control datepicker" placeholder="Select date" />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="dateDocIn">Date Doc In</label>
                                <input type="text" id="dateDocIn" name="dateDocIn" class="form-control datepicker" placeholder="Select date" />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label for="mthToDo">EST Month to do</label>
                                <input type="text" id="mthToDo" name="mthToDo" class="form-control" placeholder="Enter month" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="staff">Staff</label>
                                <input type="text" id="staff" name="staff" class="form-control" placeholder="Enter staff name" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="allocateToWkSch">Allocate to Wk Sch</label>
                                <select id="allocateToWkSch" name="allocateToWkSch" class="form-control">
                                    <option value="">Select Option</option>
                                    <option value="Yes">Yes</option>
                                    <option value="No">No</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label for="completed">Completed/Backlog</label>
                                <select id="completed" name="completed" class="form-control">
                                    <option value="">Select Status</option>
                                    <option value="Completed">Completed</option>
                                    <option value="Backlog">Backlog</option>
                                    <option value="In Progress">In Progress</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-2"></i>Cancel
                </button>
                <button type="button" class="btn btn-primary" id="saveBtn">
                    <i class="fas fa-save me-2"></i>Save
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <!-- Include DataTables -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>

    <!-- Include Datepicker -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>

    <script>
        $(document).ready(function () {
            let dataTable;
            let currentEditId = null;

            // === 公司选项缓存 ===
            let companyOptionsMap = {}; // { [id]: optionDto }

            // === 日期小工具 ===
            function toIsoYYYYMMDD(d) {
                const date = (d instanceof Date) ? d : new Date(d);
                if (isNaN(date)) return "";
                const yyyy = date.getFullYear();
                const mm = String(date.getMonth() + 1).padStart(2, '0');
                const dd = String(date.getDate()).padStart(2, '0');
                return `${yyyy}-${mm}-${dd}`;
            }
            function formatDdMMyyyy(d) {
                const date = (d instanceof Date) ? d : new Date(d);
                if (isNaN(date)) return "";
                const dd = String(date.getDate()).padStart(2, '0');
                const mm = String(date.getMonth() + 1).padStart(2, '0');
                const yyyy = date.getFullYear();
                return `${dd}/${mm}/${yyyy}`;
            }
            function formatDate(dateString) {
                if (!dateString) return '';
                const date = new Date(dateString);
                return date.toLocaleDateString('en-GB'); // DD/MM/YYYY
            }
            // YearEndMonth => 当/下一年该月最后一天
            function yearEndMonthToDateString(monthName) {
                if (!monthName) return '';
                const months = [
                    'January', 'February', 'March', 'April', 'May', 'June',
                    'July', 'August', 'September', 'October', 'November', 'December'
                ];
                const idx = months.indexOf(monthName);
                if (idx === -1) return monthName;

                const today = new Date();
                let year = today.getFullYear();
                if (idx < today.getMonth()) year += 1; // 今年已过→算明年

                const dt = new Date(year, idx + 1, 0);
                return formatDdMMyyyy(dt);
            }

            // === 载入 LLP 公司下拉 ===
            function loadCompanyOptions() {
                $.ajax({
                    url: '/api/get/llp-company-options',
                    type: 'GET',
                    success: function (res) {
                        if (!(res && res.success && Array.isArray(res.data))) return;

                        companyOptionsMap = {};
                        const $sel = $('#companySelect');
                        $sel.empty().append('<option value="">-- Select company --</option>');

                        res.data.forEach(o => {
                            // o: { id, grouping, fileNo, referral, companyName, companyNo, incorporationDate, yearEndMonth, taxIdentificationNumber, employerNumber }
                            companyOptionsMap[o.id] = o;
                            const display = o.companyNo ? `${o.companyName} (${o.companyNo})` : o.companyName;
                            $sel.append(`<option value="${o.id}">${display}</option>`);
                        });
                    },
                    error: function (xhr) {
                        console.error('Failed to load LLP company options', xhr);
                    }
                });
            }

            // === 选公司 → 自动带出所有可自动填字段 ===
            $('#companySelect').on('change', function () {
                const selectedId = $(this).val();
                if (!selectedId || !companyOptionsMap[selectedId]) {
                    // 还原为空
                    $('#grouping').val('');
                    $('#referral').val('');
                    $('#fileNo').val('');
                    $('#company').val('');
                    $('#companyClientId').val('');
                    $('#yearEnd').val('');
                    $('#incorpDate').val('');
                    $('#co').val('');
                    $('#enumber').val('');
                    $('#tINnumber').val('');
                    return;
                }

                const c = companyOptionsMap[selectedId];
                const yearEndText = yearEndMonthToDateString(c.yearEndMonth || '');
                const incorpIso = c.incorporationDate ? toIsoYYYYMMDD(c.incorporationDate) : '';

                $('#grouping').val(c.grouping || '');
                $('#referral').val(c.referral || '');
                $('#fileNo').val(c.fileNo || '');
                $('#company').val(c.companyName || '');      // 隐藏真实公司名
                $('#companyClientId').val(c.id);             // 隐藏 Client 主键
                $('#yearEnd').val(yearEndText || '');
                $('#incorpDate').val(incorpIso || '');
                $('#co').val(c.companyNo || '');
                $('#enumber').val(c.employerNumber || '');
                $('#tINnumber').val(c.taxIdentificationNumber || '');
            });

            // === DataTable ===
            initializeDataTable();
            // === 日期选择器 ===
            initializeDatePickers();
            // === 载入公司选项 ===
            loadCompanyOptions();

            // === 保存 ===
            $('#saveBtn').click(function () {
                saveBP22Record();
            });

            function initializeDataTable() {
                dataTable = $('#taskDatatable').DataTable({
                    paging: true, lengthChange: true, searching: true, ordering: true, info: true,
                    autoWidth: false, responsive: true,
                    ajax: {
                        url: '/api/bp22/get-all',
                        type: 'GET',
                        dataSrc: function (response) {
                            if (response.success) return response.data;
                            console.error('Error loading data:', response.message);
                            showAlert('Error loading data: ' + response.message, 'danger');
                            return [];
                        }
                    },
                    columns: [
                        { data: 'grouping' },
                        { data: 'refferal' },
                        { data: 'fileNo' },
                        { data: 'companyName' },
                        { data: 'yearEnd' },
                        { data: 'incorpDate', render: d => d ? formatDate(d) : '' },
                        { data: 'co' },
                        { data: 'enumber' },
                        { data: 'tiNnumber' },
                        { data: 'code' },
                        { data: 'description' },
                        { data: 'servicesType' },           // LLP 这版字段叫 servicesType
                        { data: 'activeCoActivitySize' },
                        { data: 'yEtodo' },
                        { data: 'auditDeptMth' },
                        { data: 'aDdueDate', render: d => d ? formatDate(d) : '' },
                        { data: 'extensionDate', render: d => d ? formatDate(d) : '' },
                        { data: 'dateDocIn', render: d => d ? formatDate(d) : '' },
                        { data: 'mthToDo' },
                        { data: 'staff' },
                        { data: 'allocateToWkSch' },
                        { data: 'completed' },
                        {
                            data: 'id', orderable: false,
                            render: (id) => `
                                <div class="btn-group">
                                    <button class="btn btn-sm btn-warning edit-btn" data-id="${id}" title="Edit">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button class="btn btn-sm btn-danger delete-btn" data-id="${id}" title="Delete">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>`
                        }
                    ],
                    createdRow: function (row, data) {
                        if (data.completed === 'Completed') $(row).addClass('table-success');
                        else if (data.completed === 'Backlog') $(row).addClass('table-warning');
                        else if (data.completed === 'In Progress') $(row).addClass('table-info');
                    }
                });
            }

            function initializeDatePickers() {
                $('.datepicker').datepicker({
                    format: 'yyyy-mm-dd',
                    autoclose: true,
                    todayHighlight: true
                });
            }

            // === 保存：提交隐藏的 company / companyClientId ===
            function saveBP22Record() {
                const formData = {
                    grouping: $('#grouping').val(),
                    refferal: $('#referral').val(),
                    fileNo: $('#fileNo').val(),

                    companyName: $('#company').val(),               // 真实公司名（隐藏）
                    companyClientId: $('#companyClientId').val(),   // Client 主键（隐藏）

                    yearEnd: $('#yearEnd').val(),
                    incorpDate: $('#incorpDate').val(),
                    co: $('#co').val(),
                    enumber: $('#enumber').val(),
                    tiNnumber: $('#tINnumber').val(),
                    code: $('#code').val(),
                    description: $('#description').val(),
                    servicesType: $('#servicesType').val(),
                    activeCoActivitySize: $('#activeCoActivitySize').val(),
                    yEtodo: $('#yEtodo').val(),
                    auditDeptMth: $('#auditDeptMth').val(),
                    aDdueDate: $('#aDdueDate').val(),
                    extensionDate: $('#extensionDate').val(),
                    dateDocIn: $('#dateDocIn').val(),
                    mthToDo: $('#mthToDo').val(),
                    staff: $('#staff').val(),
                    allocateToWkSch: $('#allocateToWkSch').val(),
                    completed: $('#completed').val()
                };

                const url = currentEditId ? '/api/bp22/update' : '/api/bp22/create';
                const method = currentEditId ? 'PUT' : 'POST';
                if (currentEditId) formData.id = currentEditId;

                $('#saveBtn').prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-2"></i>Saving...');

                $.ajax({
                    url, type: method, contentType: 'application/json',
                    data: JSON.stringify(formData),
                    success: function (response) {
                        $('#saveBtn').prop('disabled', false).html('<i class="fas fa-save me-2"></i>Save');
                        if (response.success) {
                            $('#taskModal').modal('hide');
                            dataTable.ajax.reload(null, false);
                            resetForm();
                            showAlert('Record saved successfully!', 'success');
                        } else {
                            showAlert('Error saving record: ' + response.message, 'danger');
                        }
                    },
                    error: function (xhr, status, error) {
                        $('#saveBtn').prop('disabled', false).html('<i class="fas fa-save me-2"></i>Save');
                        showAlert('Error saving record: ' + error, 'danger');
                    }
                });
            }

            // === 编辑：尽量回填下拉；找不到则临时插入只读项 ===
            $(document).on('click', '.edit-btn', function () {
                const id = $(this).data('id');
                editRecord(id);
            });

            function editRecord(id) {
                $.get(`/api/bp22/get/${id}`, function (response) {
                    if (!response.success) {
                        showAlert('Error loading record: ' + response.message, 'danger');
                        return;
                    }
                    const record = response.data;
                    currentEditId = record.id;

                    // 顶部只读栏位
                    $('#grouping').val(record.grouping);
                    $('#referral').val(record.refferal);
                    $('#fileNo').val(record.fileNo);

                    // 公司（隐藏真实字段）
                    $('#company').val(record.companyName || '');
                    $('#companyClientId').val(record.companyClientId || '');

                    // 下拉回选：优先按 companyClientId；否则按名称；再不行插入临时项
                    const $sel = $('#companySelect');
                    const cid = record.companyClientId;
                    if (cid && companyOptionsMap[cid]) {
                        $sel.val(String(cid));
                    } else {
                        const textMatch = $sel.find('option').filter(function () {
                            return $(this).text().trim().toLowerCase()
                                .startsWith((record.companyName || '').trim().toLowerCase());
                        });
                        if (textMatch.length > 0) {
                            $sel.val(textMatch.first().val());
                        } else if (record.companyName) {
                            if ($sel.find('option[value="__temp__"]').length === 0) {
                                $sel.prepend(`<option value="__temp__">${record.companyName}</option>`);
                            } else {
                                $sel.find('option[value="__temp__"]').text(record.companyName);
                            }
                            $sel.val('__temp__');
                        } else {
                            $sel.val('');
                        }
                    }

                    // 其他字段
                    $('#yearEnd').val(record.yearEnd || '');
                    $('#incorpDate').val(record.incorpDate ? record.incorpDate.split('T')[0] : '');
                    $('#co').val(record.co);
                    $('#enumber').val(record.enumber);
                    $('#tINnumber').val(record.tiNnumber);
                    $('#code').val(record.code);
                    $('#description').val(record.description);
                    $('#servicesType').val(record.servicesType);
                    $('#activeCoActivitySize').val(record.activeCoActivitySize);
                    $('#yEtodo').val(record.yEtodo);
                    $('#auditDeptMth').val(record.auditDeptMth);
                    $('#aDdueDate').val(record.aDdueDate ? record.aDdueDate.split('T')[0] : '');
                    $('#extensionDate').val(record.extensionDate ? record.extensionDate.split('T')[0] : '');
                    $('#dateDocIn').val(record.dateDocIn ? record.dateDocIn.split('T')[0] : '');
                    $('#mthToDo').val(record.mthToDo);
                    $('#staff').val(record.staff);
                    $('#allocateToWkSch').val(record.allocateToWkSch);
                    $('#completed').val(record.completed);

                    $('#modalTitle').text('Edit LLP Record');
                    $('#taskModal').modal('show');
                }).fail(function (xhr, status, error) {
                    showAlert('Error loading record: ' + error, 'danger');
                });
            }

            // === 删除 ===
            $(document).on('click', '.delete-btn', function () {
                const id = $(this).data('id');
                deleteRecord(id);
            });
            function deleteRecord(id) {
                if (!confirm('Are you sure you want to delete this record? This action cannot be undone.')) return;
                $.ajax({
                    url: `/api/bp22/delete/${id}`, type: 'DELETE',
                    success: function (response) {
                        if (response.success) {
                            dataTable.ajax.reload(null, false);
                            showAlert('Record deleted successfully!', 'success');
                        } else {
                            showAlert('Error deleting record: ' + response.message, 'danger');
                        }
                    },
                    error: function (xhr, status, error) {
                        showAlert('Error deleting record: ' + error, 'danger');
                    }
                });
            }

            // === 表单/提示 ===
            function resetForm() {
                $('#taskForm')[0].reset();
                currentEditId = null;
                $('#modalTitle').text('Add New LLP Record');
                $('.datepicker').datepicker('update', '');
                $('#recordId').val('');
                $('#companySelect').val('');
                $('#company').val('');
                $('#companyClientId').val('');
            }
            function showAlert(message, type) {
                const alertClass = `alert alert-${type} alert-dismissible fade show`;
                const alertHtml = `
                    <div class="${alertClass}" role="alert">
                        <i class="fas ${type === 'success' ? 'fa-check-circle' : type === 'danger' ? 'fa-exclamation-circle' : 'fa-info-circle'} me-2"></i>
                        ${message}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>`;
                $('#alertContainer').html(alertHtml);
                setTimeout(() => { $('.alert').alert('close'); }, 5000);
            }

            // 模态关闭重置
            $('#taskModal').on('hidden.bs.modal', function () {
                resetForm();
            });

            // 新增按钮
            $('#addNewBtn').click(function () {
                resetForm();
                $('#modalTitle').text('Add New LLP Record');
            });

            // 表单提交
            $('#taskForm').on('submit', function (e) {
                e.preventDefault();
                saveBP22Record();
            });
        });
    </script>

}